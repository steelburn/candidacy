# ============================================
# Candidacy Platform - Environment Configuration
# ============================================
# This is the ONLY .env.example file for the entire platform.
# Copy this to .env and configure for your environment.
#
# NOTE: Application-level configurations (AI models, features, etc.)
# are now managed via the database. See CONFIGURATION.md for details.
# ============================================

# ============================================
# GLOBAL SETTINGS
# ============================================
APP_ENV=local
APP_DEBUG=true

# Security Keys (auto-generated by setup-env.sh if not present)
# APP_KEY: Laravel application encryption key
# JWT_SECRET: JWT authentication secret
# Run 'make setup' or 'bash scripts/setup-env.sh' to generate these automatically

# ============================================
# DATABASE CONFIGURATION
# ============================================
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=root

# Service-specific databases (auto-created by init-databases-from-dbml.sh)
# - candidacy_auth
# - candidacy_admin
# - candidacy_candidate
# - candidacy_vacancy
# - candidacy_ai
# - candidacy_matching
# - candidacy_interview
# - candidacy_offer
# - candidacy_onboarding
# - candidacy_document_parser

# ============================================
# CACHE & QUEUE
# ============================================
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis

# ============================================
# REDIS CONFIGURATION
# ============================================
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# ============================================
# LOGGING
# ============================================
LOG_CHANNEL=stack
LOG_LEVEL=debug

# ============================================
# SERVICE URLS (Internal Docker Network)
# ============================================
# These are used for inter-service communication
ADMIN_SERVICE_URL=http://admin-service:8080
CANDIDATE_SERVICE_URL=http://candidate-service:8080
VACANCY_SERVICE_URL=http://vacancy-service:8080
AI_SERVICE_URL=http://ai-service:8080

# ============================================
# APPLICATION CONFIGURATIONS
# ============================================
# All application-level configurations have been moved to the database
# and are managed via the Admin Service Configuration API.
#
# To configure the application:
# 1. Start the services: docker-compose up -d
# 2. Seed configurations: docker-compose exec admin-service php artisan db:seed --class=ConfigurationSeeder
# 3. Update via API: curl -X PUT http://localhost:8090/api/settings -d '{"key": "value"}'
#
# Configuration categories in database:
# - system: app name, company, contact, timezone, language
# - ai: provider, Ollama URL/models, OpenRouter settings
# - document_parser: Granite Docling settings, timeout, resolution
# - recruitment: auto-matching, offer expiry, interview reminders
# - storage: CV size limit, max upload size
# - features: enable AI, notifications, auto-matching
# - services: internal service URLs
#
# See CONFIGURATION.md for complete reference of all 27 settings.
#
# Example configurations (now in database):
# - ai.ollama.url = http://192.168.88.120:11434
# - ai.ollama.model.default = mistral
# - document_parser.use_granite_docling = true
# - features.enable_ai = true
# - recruitment.auto_matching = true
